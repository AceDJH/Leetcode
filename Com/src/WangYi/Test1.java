package WangYi;

/**
 * @Author AceDJH
 * @Date 2020/11/17 19:46
 * 非递归中序遍历二叉树
 */
public class Test1 {
/*   public List mid(TreeNode treeNode){
       Stack stack = new Stack();
       if (treeNode == null){
           return null;
       }
       List<TreeNode> treeNodes = new ArrayList<>();
       while (treeNode.left != null){
           stack.push(treeNode);
           treeNode = treeNode.left;
       }
       stack.push(treeNode);
       treeNodes.add(stack.pop());
       if (treeNode.right != null){
           treeNode = treeNode.right;
           while (treeNode.left != null){
               stack.push(treeNode);
               treeNode = treeNode.left;
           }
           stack.push(treeNode);
           treeNodes.add(stack.pop());
       }

   }*/
}
